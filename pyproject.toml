[project]
name = "ai-invest-copilot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sueyyy", email = "sueyy36@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ai-invest-copilot = "ai_invest_copilot:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
